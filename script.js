const pokeNameIndex = document.querySelector('#name-index');const extraInfo = document.querySelector('#extra-info');const moves = document.querySelector('#moves');const image = document.querySelector('#image');// SETUP INPUT FIELDS AND BUTTONSconst searchNameButton = document.querySelector('#search-button');searchNameButton.addEventListener('click', search);function search(){    const input = document.querySelector('#input-value');    const inputValue = input.value.toLowerCase();    async function getData(input){        const data = await fetch(`https://pokeapi.co/api/v2/pokemon/${input}`);        const pokemon = await data.json();        pokeNameIndex.innerHTML = pokemon.name.toUpperCase() + '<br>' + '#' + pokemon.id;        image.src = pokemon.sprites.front_default;        extraInfo.innerHTML = pokemon.flavor_text_entries.flavor_text;        for(let i = 0; i < moves.length; i++){            let i = Math.floor(Math.random()*4);            moves.innerHTML = pokemon.moves[i].move.name;        }    }    getData(inputValue);}